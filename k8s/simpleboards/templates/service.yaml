apiVersion: v1
kind: Service
metadata:
  name: {{ include "simpleboards.fullname" . }}
  labels:
    {{- include "simpleboards.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "simpleboards.selectorLabels" . | nindent 4 }}
